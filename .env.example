# Optional dev-only fallback for Telegram API credentials.
# Installed app: use App Settings → Telegram instead (stored in %APPDATA%\LedgerLink\profiles.json).
# Get credentials from https://my.telegram.org/apps

TELEGRAM_API_ID=
TELEGRAM_API_HASH=
